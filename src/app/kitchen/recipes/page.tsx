import Image from "next/image";

export default function Home() {
  return (
    <div className="h-full w-full bg-[var(--background)]">
      <section>Proximamente Recetas</section>
    </div>
  );
}
