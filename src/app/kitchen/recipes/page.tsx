import Image from "next/image";

export default function Home() {
  return (
    <div className="h-full w-full bg-[var(--background)] flex items-center justify-center text-black">
      <section className="text-4xl md:text-6xl font-bold text-center">
        Próximamente Recetas
      </section>
    </div>
  );
}
